---
layout: default
---
<!--
  Custom Publications Layout

  This template was created to convert the publications page from manual HTML to proper Jekyll templating.
  Previously, each publication was manually coded as HTML divs. Now they are stored as structured YAML data
  in the page's front matter and rendered dynamically.

  Key features:
  - Uses existing CSS classes (.publication-entry, .publication-image, .publication-info) for consistent styling
  - Loops through page.publications array from YAML front matter
  - Maintains exact same visual appearance while following Jekyll best practices
  - Makes adding new publications much easier (just add YAML data vs copying HTML)

  Usage: Set layout: publications in page front matter and provide publications array with:
  - title: Publication title and citation
  - url: Link to paper/preprint
  - image: Path to publication image
  - alt: Alt text for image
  - description: Publication description/abstract
-->

<div class="post">
  <header class="post-header">
    <h1 class="post-title">
      {% if site.enable_navbar_social %}
        {{ page.title }}
        <div class="social">
          <div class="contact-icons">
            {% include social.liquid %}
          </div>
        </div>
      {% else %}
        {{ page.title }}
      {% endif %}
    </h1>
    <p class="post-description">{{ page.description }}</p>
  </header>

  <article>
    {% if page.publications %}
      {% for pub in page.publications %}
        <div class="publication-entry">
          <div class="publication-image">
            <img src="{{ pub.image | relative_url }}" alt="{{ pub.alt }}" class="img-fluid">
          </div>
          <div class="publication-info">
            <a href="{{ pub.url }}">{{ pub.title }}</a>. {{ pub.description }}
          </div>
        </div>
      {% endfor %}
    {% endif %}

    {{ content }}
  </article>
</div>
